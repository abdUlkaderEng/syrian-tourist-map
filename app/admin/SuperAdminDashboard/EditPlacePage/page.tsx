import React from "react";

const EditPlacePage = ({ params }: { params: { id: string } }) => {
  // TODO: Implement edit form for place with id params.id
  return <div>Edit Place (ID: {params.id})</div>;
};

export default EditPlacePage;
